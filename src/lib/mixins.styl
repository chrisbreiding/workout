hover() {
  &:hover,
  &:focus,
  &:active {
    {block};
  }
}

rounded-button() {
  border-radius: 3px;
  padding: 0.4em 0.6em;
}

absolute-full(margin) {
  bottom: margin;
  left: margin;
  position: absolute;
  right: margin;
  top: margin;
}
